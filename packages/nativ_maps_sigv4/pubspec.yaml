name: nativ_maps_sigv4
description: "AWS Signature Version 4 credentials for nativ_maps, verified against the official AWS test vectors and depending only on package crypto."
version: 0.3.0
repository: https://github.com/delioribas/nativ_maps
issue_tracker: https://github.com/delioribas/nativ_maps/issues
publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  crypto: ^3.0.7
  http: ^1.1.0
  meta: ^1.16.0
  nativ_maps: ^0.3.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
